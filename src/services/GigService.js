
class GigService {

  constructor(models) {
    this.models = models;
  }

  async notifyJobSeekers({ professionId, user })=> {
   const {employeeProfession,notified} this.models
     console.log({ professionId, user })
   }

 }

